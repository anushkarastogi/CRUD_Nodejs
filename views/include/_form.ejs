 <!-- form handling -->
 <form action="/api/users" method="POST" id="add_user">
    <div class="new_user">
        <div class="form-group">
            <label for="name" class="text-light">Name</label>
            <input type="hidden" name="id" value="<%= user._id %>">
            <input type="text" name="name" value="<%= user.name %>" placeholder="Mark Stoenis">
        </div>
        <div class="form-group">
            <label for="Age" class="text-light">Age</label>
            <input type="text" name="age" value="<%= user.age%>" placeholder="Age">
        </div>
        <div class="form-group">
            <label for="Phoneno" class="text-light">Phone No</label>
            <input type="text" name="phone" value="<%=user.phone%>" placeholder="Phone No">
        </div>

        <div class="form-group">
            <label for="experience" class="text-light">Experience</label>
            <input type="number" name="exp" value="<%user.exp%>" placeholder="Exp">
        </div>

        <div class="form-group">
            <label for="salary" class="text-light">Department</label>
            <input type="department" name="department" value="<%=user.department%>" placeholder="department">
        </div>

        <div class="form-group">
            <label for="salary" class="text-light">Salary</label>
            <input type="salary" name="salary" value="<%=user.salary%>" placeholder="Salary">
        </div>

        <div class="form-group">
            <button type="submit" class="btn text-dark update">Save</button>
        </div>

    </div>
</form>